import React from "react";

import { Layout, Ecosystem } from "../components";

const EcosystemPage = (props) => (
  <Layout {...props}>
    <Ecosystem />
  </Layout>
);

export default EcosystemPage;
